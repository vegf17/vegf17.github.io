# Jekyll files for quantUM group using the Alembic theme

Starter kit for [Alembic](https://alembic.darn.es/).

Commands to test changes locally:
	 1. chruby 3.2.6 (selects the ruby version)
	 2. bundle exec jekyll serve
